{{ form_start(form, {'attr': {'class' : 'd-flex'}}) }}
    {#{{ form_widget(form) }}#}
    {{ form_row(form.title) }}
    <button class="btn btn-primary">{{ button_label|default('Add') }}</button>
{{ form_end(form) }}
